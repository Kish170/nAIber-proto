[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/llm-server/Dockerfile"
buildContext = "."

[deploy]
startCommand = "node apps/llm-server/dist/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
